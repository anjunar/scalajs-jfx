{
  "name": "scala-js-simplicity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:ssr": "node server/ssr-dev-server.mjs",
    "build": "vite build && node scripts/generate-static-routes.mjs",
    "build:ssr": "vite build --ssr && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@scala-js/vite-plugin-scalajs": "^1.1.0",
    "express": "^4.18.2",
    "happy-dom": "^20.8.9",
    "vite": "^8.0.0"
  }
}
